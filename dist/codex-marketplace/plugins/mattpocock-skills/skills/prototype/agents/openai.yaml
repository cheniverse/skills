interface:
  display_name: "Matt: prototype"
  short_description: "Build a throwaway prototype to answer a design question."
  default_prompt: "Use $prototype to work with Matt's prototype skill."
policy:
  allow_implicit_invocation: true
