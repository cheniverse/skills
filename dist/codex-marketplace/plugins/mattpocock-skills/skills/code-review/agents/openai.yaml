interface:
  display_name: "Matt: code-review"
  short_description: "Review a requested diff, branch, PR, or work in progress for..."
  default_prompt: "Use $code-review to work with Matt's code-review skill."
policy:
  allow_implicit_invocation: true
