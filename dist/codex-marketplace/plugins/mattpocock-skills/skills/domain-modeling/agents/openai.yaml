interface:
  display_name: "Matt: domain-modeling"
  short_description: "Build and sharpen a project's domain model."
  default_prompt: "Use $domain-modeling to work with Matt's domain-modeling skill."
policy:
  allow_implicit_invocation: true
