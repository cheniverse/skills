interface:
  display_name: "Matt: wizard"
  short_description: "Generate an interactive bash wizard that walks a human throug..."
  default_prompt: "Use $wizard to work with Matt's wizard skill."
policy:
  allow_implicit_invocation: false
